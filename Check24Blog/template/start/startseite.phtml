<?php foreach ($blogEntries as $blogEntry) : ?>
    <div id="artikel">
        <article class="w3-border w3-blue w3-padding"><p>Heute: <?= $blogEntry['title'] ?> </p></article>
        <br/>
        <article class="w3-border w3-blue w3-padding"><?= $blogEntry['text'] ?> </article>
        <br/>
        <article class="w3-border w3-blue w3-padding"><p>Autor: <?= $blogEntry['author'] ?></p></article>
    </div>
<?php endforeach; ?>
